version: 2

run:
  deadline: 5m
  tests: true
  skip-dirs:
    - vendor/
    - node_modules/

linters:
  disable-all: true
  enable:
    - errcheck
    - govet
    - staticcheck
    - ineffassign
    - unused

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck